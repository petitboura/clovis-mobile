{
  "name": "clovis-mobile-capacitor",
  "version": "0.1.0",
  "private": true,
  "description": "Conteneur Capacitor autour de clovis-frontend (Phase 3, fusion mobile). Le code natif historique des Lots 1-3 est preserve dans android-legacy-natif/ et ios-legacy-natif/ en attendant sa migration en plugins (Lot 3B).",
  "scripts": {
    "build:web": "cd web && npm install && npm run build",
    "sync": "npx cap sync",
    "build": "npm run build:web && npm run sync"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0",
    "@capacitor/ios": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
